import { SlideChangeDirective } from './slide-change.directive';

describe('SlideChangeDirective', () => {
  it('should create an instance', () => {
    const directive = new SlideChangeDirective();
    expect(directive).toBeTruthy();
  });
});
